/**
 * Stack Layout Components
 *
 * Re-exports VStack and HStack for convenient importing.
 *
 * @example
 * import { VStack, HStack } from './components/Stack';
 */

export { VStack, type VStackProps, type SpaceValue } from './VStack';
export { HStack, type HStackProps, type AlignValue, type JustifyValue } from './HStack';
