/**
 * ID generation utilities
 *
 * Re-exports from mocks/generators/ids for use in scenarios and other modules.
 */

export {
  generateId,
  generateMRN,
  generateNPI,
  generateRequisitionId,
  generateEncounterId,
  generatePatientId,
  generateItemId,
  generateSuggestionId,
  generateTaskId,
  generateCareGapId,
  generateUserId,
} from '../mocks/generators/ids';
